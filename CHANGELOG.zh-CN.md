# 变更日志（Changelog）

SwitchAI 的所有重要变更将记录在此文件中。

> **注意：** 本文档为中文版，是变更日志的主要参考文档。英文文档（[CHANGELOG.md](./CHANGELOG.md)）据此同步生成。

## [1.0.3] - 2026-05-29

### 新增功能

- **内置聊天沙盒（Playground）** — 侧边栏新增「Playground」入口，提供应用内聊天测试环境。可选择任意已配置模型、发送消息（支持流式响应），并在右侧面板实时观察路由决策链路，无需借助任何外部客户端工具。
- **实时路由模拟器** — Playground 右侧新增可折叠的「实时路由模拟器」面板，无需发送真实请求，即可在选择模型或修改配置的瞬间预览完整的路由决策路径（网关入口模式 → 别名重写 → 路由策略 → 调度队列排序）。
- **路由诊断链路（Trace Timeline）** — 每次请求后展示完整的诊断面板，包括：总体结果横幅（成功/全败）、总耗时、各阶段耗时分布彩色条，以及每次尝试的供应商名称、HTTP 状态码、响应时间、完整端点 URL 和错误信息。
- **供应商延迟趋势图（Sparkline）** — 提供商列表新增「响应延迟」列，展示近 10 次健康拨测的平均延迟徽章与 SVG 折线趋势图。
- **高级路由策略** — 新增两种模型级路由策略：**最低延迟优先**（按健康拨测的平均延迟升序排序供应商）与**最低计费优先**（按定价配置估算的每千 Token 综合单价升序排序）。
- **路由规则通配符匹配** — 模型路由策略规则现支持通配符模式（如 `gpt-*`、`*`）。首次配置时自动填入默认的 `*` → 故障转移规则。
- **全局日志模糊搜索** — 日志页面搜索框现可跨多字段全文模糊匹配：模型名、供应商、HTTP 状态码、错误信息、请求体、响应体、客户端密钥名和端点 URL。
- **模型定价配置** — 系统设置新增「模型定价」区域，支持按模型（含通配符）分别配置输入/输出每千 Token 单价，用于仪表盘成本估算和「最低计费」路由策略。
- **客户端 API 密钥管理** — 系统设置新增客户端 API 密钥管理模块，可为本地网关创建多个命名密钥，并单独开启/关闭，实现访问权限管控。
- **网关模式快捷切换** — Playground 和仪表盘新增内联网关模式切换器（直连 / 统一），修改后实时持久化保存。
- **模型路由规则默认配置** — 首次启动或无路由规则时，自动填充默认的 `*` → 故障转移通配符规则。

### 修复

- **路由规则保存失败** — 修复了一个关键的序列化不匹配问题：前端发送的是 `"lowestlatency"` / `"lowestcost"`，而 Rust serde 期望的是 `"lowest_latency"` / `"lowest_cost"`，导致反序列化静默失败，整个 `routing` 配置在保存时被丢弃。
- **模型别名映射保存失败** — 同样的序列化路径问题导致模型别名映射有时无法在重启后持久化。
- **通配符路由规则不生效** — `router.rs` 在匹配路由规则时使用的是精确字符串比较（`r.model == model_name`），导致 `*`、`gpt-*` 等通配符规则永远无法命中。已改用 `matches_wildcard()` 函数进行匹配。
- **Trace 面板策略名称显示错误** — `X-SwitchAI-Trace` 响应头中的 `routing_strategy` 使用 Rust `Debug` 格式序列化（如 `"Failover"`），而非 serde JSON 名称（如 `"failover"`），导致 Playground 显示的策略名与实际不符。
- **「收起配置」按钮显示为英文** — `translations.ts` 中两个语言块均缺少 `models.collapseRouting` 翻译键，导致折叠按钮始终回退为硬编码英文字符串。
- **供应商页面开启网关后 Item 样式变形** — 延迟 Sparkline 列出现时，提供商表格列宽被撑开错位。已为各列设置固定宽度，并将 SVG 约束在 60px 内。
- **仪表盘 API 网关入口切换不生效** — 点击模式切换后立即弹出「保存成功」提示，但实际未应用新值。保存处理函数现已在持久化前正确读取新模式值。
- **模型路由页面界面切换为英文** — 修复了缺失的 `models.collapseRouting` 翻译键。
- **输入框提示文字包含 `e.g.` 前缀** — 全局清理了所有输入框占位提示文字中的 `e.g.` 前缀，改为直接展示示例文本。
- **系统设置中的「Token 单价」字段** — 已从系统设置服务器区域删除该过时字段；定价配置现统一由「模型定价配置」管理。

### 改进

- 提供商表格各列增加固定宽度，防止健康数据加载时页面布局抖动。
- Playground 右侧面板采用双区布局：上方为可折叠的实时路由模拟器，下方为常驻路由追踪诊断面板。
- 路由追踪面板新增摘要横幅、彩色耗时分布条，尝试详情卡片中端点 URL 始终展示，无需点击展开。
- 模型路由页面策略下拉选项在中文模式下正确显示中文名称。

---

## [1.0.2] - 2026-05-26


### 新增功能

- **模型别名与重定向映射** — 增加模型别名（Model Aliasing）重定向重写机制，支持在“模型路由”页面直观地配置并持久化别名，静默将客户端请求转换为后端目标模型。

### 修复与优化

- **流式分包截断修复** — 使用字节缓冲区流式行解析重构 SSE，彻底解决大文本或中文字符跨 TCP 包被截断导致乱码的缺陷。
- **异步日志 Worker** — 日志记录落库机制重构为异步 Worker 通道，避免并发请求时 SQLite 写锁阻塞转发主线程。
- **并发健康检查** — 定时服务商探测改为异步并发机制，防止慢节点或超时卡住全局健康轮询。
- **提升稳定性** — 提高网关 API 请求体限制至 50MB 以兼容复杂上下文提问，并调整崩溃日志（`crash.log`）生成路径为用户家目录，防止 Windows 下权限不足导致网关二次闪退。

---

## [1.0.1] - 2025-07-23

### 修复

- **健康检查 503 错误** — 修复健康检查未携带 API Key 认证，导致 OpenAI 等认证制提供商在 15 分钟后被错误标记为 unhealthy，所有请求返回 "No enabled provider with models available" (503)
- **仪表盘 Token 统计不显示** — 修复 `add_log` 使用 `try_lock()` 在 Dashboard 轮询时静默丢失请求日志，改为阻塞 `lock()` 确保数据持久化
- **配置加载容错** — `get_stats` 命令在配置加载失败时不再整体报错，降级使用默认值，避免仪表盘空白
- **白屏闪退** — 修复 `ErrorBoundary` 导入了不存在的 `useTranslation`（应为 `useI18n`），以及 `translations.ts` 重复 key 导致的编译错误

### 改进

- Dashboard 统计请求失败时显示可见的红色错误横幅，并输出 `console.error` 便于调试
- `getInvoke()` helper 函数在 store 中缓存动态 import，避免 Tauri API 模块初始化时机问题
- 健康检查循环复用单个 `reqwest::Client` 实例，避免每次检查时重复创建连接池

### CI / 文档

- GitHub Actions Release 工作流支持 Windows / macOS / Linux 三平台并行构建
- 采用 artifact 聚合模式避免并行构建时 Release 冲突
- 完善开源项目文件：AGPL-3.0 许可证、中英双语文档、Issue/PR 模板、贡献指南

---

## [1.0.0] - 2025-07-23

### 新增功能

- 多提供商管理，支持 OpenAI 兼容 API
- 智能路由：故障转移和负载均衡策略，支持按模型配置
- OpenAI 兼容 HTTP 网关（`/v1/models`、`/v1/chat/completions`）
- 流式传输（SSE）支持
- 多模态请求自动检测与路由
- 定时提供商健康检查（每 5 分钟），自动标记不健康节点
- 仪表盘：Token 用量统计、费用估算、流量图表
- 请求日志：搜索、筛选、JSON/CSV 导出
- 系统托盘：快速启停网关
- 深色模式（浅色/深色/系统）
- 国际化（中文/英文）
- 开机自启和网关自启选项
- 错误边界组件，防止页面崩溃
- SQLite 数据库，支持自动迁移升级
- 操作系统密钥环集成，安全存储 API 密钥
- 可配置的日志保留天数和 Token 计价
- Windows / macOS / Linux 三平台支持

### 格式说明

本日志遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.0.0/) 规范。
